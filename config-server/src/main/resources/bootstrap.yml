spring:
  application:
    name: config-server 
  cloud:
    config:
      server:
        git:
          uri: https://github.com/linfan/spring-cloud-demo.git
          searchPaths: config-repo

server:
  port: 8888